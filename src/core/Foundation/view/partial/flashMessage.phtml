<?php
/**
 * @var array $error [ 'message'=>'', 'type'=>'', 'meta'=> [] ]
 */
$messages = get_defined_vars();

foreach ($messages as $type => $tMess) {
    foreach ($tMess as $m) {
        $css = ($type == 'error') ? 'danger' : $type;
        ?>
        <div class="alert alert-<?php echo $css ?>" role="alert">
            <?php echo $m['message']; ?>
        </div>
    <?php }
}
